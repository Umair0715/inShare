extends baseEmail 
block content 
  p Hi #{emailTo},
  p #{emailFrom} has shared a File with you! 🎉🙏
  p #{size} in size . Expires in #{expires}
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  a.downloadFileBtn(href=`${downloadLink}`, target='_blank') Download File 
  p Thank You for using InShare Service
  p - Good Luck . Hope its work!    